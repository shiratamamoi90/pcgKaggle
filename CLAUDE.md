# CLAUDE.md — ポケカ AI Battle Challenge

> **このファイルは2部構成。**
> **第1部（2026-08-17〜）= 現在参加中の Strategy カテゴリ。**
> **第2部 = 終了した Simulation カテゴリの記載。削除せず全文保持する**
> （Strategy が終わり次第、両方まとめて整理する — ユーザ指示 2026-08-17）。

---

# 第1部：現在の作業 — Strategy カテゴリ（2026-08-17〜）

## 現在地

**Simulation カテゴリは 2026-08-16 23:59 UTC に締切を迎えて終了した。**

- 最終結果: **846.1 / 673位 6,892チーム中（上位 9.8%）**、提出総数 50。
  ※締切後もラダーは対戦を続けているため**順位は暫定**（08-17 11:23 のスナップショット。
  確認中も 846.1 → 841.6 → 835.5 と動いた）。
- **エージェント側の作業は全て終了。** 提出はもう受け付けられない。
  訓練・評価・提出tarのビルドを新たに走らせる理由はない。

**このリポジトリの現在の目的は Strategy カテゴリへの技術レポート提出である。**

- コンペ: https://www.kaggle.com/competitions/pokemon-tcg-ai-battle-challenge-strategy
- 締切: **2026-09-13 23:59 UTC**
- 提出形式: **Kaggle Writeup**（技術レポート）＋ メディアギャラリー添付。リーダーボードなし・審査制
- 賞金: 上位8チームに各 **$30,000**（総額 $240,000）＋ 第2ラウンド出場権
- 参加資格: Simulation カテゴリへの参加が必須 → **充足済み**
- **我々はまだ Strategy に参加登録していない**（Rules 同意が必要）

## 最初に読むもの

1. **`docs/README.md`** — 現在地・最終結果・Strategy の仕様・資産の一覧
2. **`docs/FINDINGS.md`** — **レポートの骨格**。何を測り何が分かったか（主張は全て数字と紐づけ済み）
3. **`docs/CHRONICLE.md`** — **全48施策の詳細記録**。
   1件ごとに「目標 / 実施 / 成果・改善または不具合・悪化 / 次に取った手順」を時系列で記載
4. `docs/LINEAGE.md` — 技術の正典（999行）。全世代の中身・復元手順・死んだ軸の台帳。
   **③k を最初に読む**（他の節の多くを無効化している）
5. `docs/spec.md` / `docs/procedure.md` / `docs/eval-axes.md` — Simulation 用の記載。
   **役目は終えたが削除しない。** レポートで当時の設計意図を引くときに使う

## レポートを書くときの原則

**このプロジェクトの提出可能な価値は「強いエージェント」ではなく、
「何が効かないかを対照群を置いて50提出ぶん測り切った記録」の側にある。**
673位のエージェントを強かったことにする書き方はしない。

1. **数字の裏付けなしに主張を書かない。** `docs/FINDINGS.md` §7 に主張候補と根拠の対応がある。
2. **測定の失敗も書く。** 42.4pp の測定エラー・沈黙する初期化バグ・cwd バグ・
   蒸留ターゲットが生徒より弱かった件は、いずれも再現性のある一般的な教訓になっている
   （FINDINGS §4）。最終ペアが平均から 3.7σ 下に着地した件も同様に書く。
3. **閉じた軸を再挑戦しない。** `docs/FINDINGS.md` §3 の台帳を先に見る。
   作る側の軸は 2026-08-09 時点で全て閉鎖済み。
4. 過去の数値を引用する前に、それが**どの測定器で取られたか**を確認する。
   `opponents/grimmsnarl` 由来の数値は無効（07-06製の古い方策）。有効なのは `opponents/metapool`。

## 引き継がれる制約

- 公式エンジン・カードデータ・サンプルは**再配布禁止**。リポジトリにコミットしない。
- 複数Kaggleアカウント禁止。
- `episodes/`（145GB）は**削除しない**。実ラダー7日分の一次データで、Strategy レポートの
  メタ分析の材料であり、他チームが持っていない規模。

## ディレクトリ構成（2026-08-17 現在）

```
.
├── CLAUDE.md                       # このファイル
├── docs/
│   ├── README.md                   # 現在地・最終結果・Strategy 仕様
│   ├── FINDINGS.md                 # ★レポートの骨格
│   ├── LINEAGE.md                  # 技術の正典
│   ├── spec.md / procedure.md / eval-axes.md   # Simulation 用（保持）
├── episodes/                       # 実ラダーリプレイ7日分 145GB（一次データ・削除禁止）
├── models_bc/ models_deploy/ models_soup2/ models_majkel_rl/
├── opponents/                      # 評価用エージェント（metapool のみ有効）
├── cg/                             # 公式エンジン（.gitignore対象）
├── tools/ scratchpad/*.py          # 訓練・評価・分析スクリプト
├── logs/                           # 訓練ログ（主張の証拠）
├── venv/                           # 分析基盤
└── submission_*.tar.gz             # 各世代の提出物6本（物証）
```

---
---

# 第2部：Simulation カテゴリの記載（2026-06-20〜2026-08-16・終了）

> **以下は当時の記載を一字も変えずに保持したもの。**
> 提出はもう受け付けられないため、ここの提出手順・提出制約は**実行対象ではない**が、
> Strategy レポートで当時の運用方針を引用するための一次記録として残す。

## このプロジェクトのゴール

- フェーズA: 既存サンプルのルールベースエージェントを流用し、valid submission を1回通す
- フェーズB: 自己対戦の評価スクリプトで「勝率・平均ターン数・先後別・サイド差」を計測し、
  改良 → A/Bテスト → 再提出 のループを回す

エージェントは**ゼロから書かない**。既存のサンプル（メガルカリオex / Iono等）を流用し、
小さく1点ずつ改良する方針。

## 絶対に守る制約

- 順位は Elo（勝敗のみ）で決まる。細かい評価軸は**自分で計測する**もので、公式採点ではない。
- 提出物は `main.py`（トップレベル）と `deck.csv` を含む `submission.tar.gz`。**ネスト禁止**。
- 1日最大5提出 / 追跡は最新2提出 / 期間は2026-08-17まで（最終提出は8/16）。
- **【絶対厳守】3本以上を一度に投げない。** 追跡は最新2提出なので、3本目を投げた時点で1本目が追跡から外れる。
- 提出は原則2本ずつ（ペア＝[実績tarの再roll＝床] + [挑戦者]）で行い、**差分**で判定する。
- **ただし次のどちらかに当てはまるときは1本だけの提出でよい（2026-07-21 ユーザ指示）:**
  1. **変更が1種類だけ**のとき（例: model.pth だけ差し替え等、単一変数の変更）
  2. **残りの提出枠が1つだけ**のとき
  - 1本だけ投げる場合の注意: 追跡 latest-2 は「直前の1本 + 新しい1本」になる。**直前の1本が床（実績スコアのtar）でないと対照が消えて差分が読めなくなる**ので、投入前に「今 latest-2 に残るのは何か」を必ず確認する。
- 複数Kaggleアカウント禁止。
- 公式エンジン・カードデータ・サンプルは**リポジトリにコミットしない**（再配布禁止）。
  `.gitignore` で除外し、各自Kaggleからダウンロードして配置する。

## 【最優先】測定のルール（2026-08-09 に42.4ppの測定エラーが発覚した結果）

**この節を無視した測定は全部無効になる。過去2ヶ月ぶんの結論が実際にそうなった。**

1. **相手エージェントは「デッキ」でなく「方策の日付」を見る。** 起動前に必ず `ls -la opponents/*/`。
   `opponents/grimmsnarl` は07-06製で、**デッキは現行メタと58/60一致していたため古さが見えず**、
   シミュレータは実ラダーと **42.4pp** ずれていた（champion 86.0% vs 実測43.6%）。
   → **評価に使ってよいのは `opponents/metapool`**（実ラダーとの差0.7pp）。`opponents/grimmsnarl` は使用禁止。
2. **オフライン指標は実ラダーの実測に照らして検証する。** `scratchpad/archetype_matrix.py` が
   エピソードからアーキタイプ間の実勝率を出す（我々のチームは不要なので数万試合が使える）。
   **これが唯一、測定器の妥当性を外部から確かめる手段。**
3. **世代・候補の選抜は必ず「新規試合で再現テスト」する。** n=200 の sd は約3.5pp あり、
   6点引けば ±7pp 級の外れ値が1つ出る。08-09 に +9.6pp が n=600 で +1.2pp に回帰した実例あり。
4. **tar の中身を主張する前に `tar -xzOf <tar> main.py` で実物を見る。**
   作業用 `main_nn.py`（`NN_SEARCH=30`）と出荷物（`=1`）は別物。取り違えた実例あり。
5. **評価スクリプトの二重起動を防ぐ。** 起動前に `ps -eo pid,cmd | grep h2h_vs_rule`。
   同一モデル同一相手で 56.6% と 41.0% が記録された実例あり（CPU奪い合い＋同一ファイル追記）。
   **ログファイルが未作成でも「起動失敗」と即断しない**（作成が遅れるだけのことがある）。
6. **モデルを読んだら重みの一致を確認する。** `main_nn.py` は load_state_dict 失敗を握り潰して
   **ランダム初期化のまま推論する**。テンソル単位で元ファイルと比較すること。

## 作業の進め方（Claude Codeへの指示）

1. 新しい作業に入る前に **`docs/LINEAGE.md` の ③k を最初に読む**（他の節の多くを無効化している）。
   次いで `docs/spec.md` と `docs/procedure.md`。
2. エージェントのロジックを変更したら、必ず**上記の有効な測定器で**旧版とA/Bテストしてから提出を提案する。
3. 数字の裏付けなしに「強くなった」と判断しない。**単発ペアのラダー検出下限は ~76 Elo (2σ)**
   なので、それ未満の差は「符号を当てた」と言ってはいけない。
4. 提出コマンドを実行する前に、`submission.tar.gz` の中身がネストしていないか `tar -tzf` で確認する。
5. **閉じた軸を再挑戦しない。** `docs/LINEAGE.md` ④ の台帳を先に見る。
   2026-08-09 時点で**作る側の軸は全て閉鎖済み**（学習系は replay 量で頭打ち、
   データ量非依存だった最後の軸=リーサル上書きも有意に有害と判明）。
   **残っている唯一のプラス行動は max-of-2 サーフィンと終盤タイミング。**

## ディレクトリ構成（想定）

```
.
├── CLAUDE.md            # このファイル
├── docs/
│   ├── spec.md          # 仕様
│   ├── procedure.md     # 手順（フェーズA / B）
│   └── eval-axes.md     # 評価軸の定義
├── main.py              # agent(obs_dict)->list[int]（サンプル流用）
├── deck.csv             # カードID 60行
├── tools/
│   └── selfplay_eval.py # 自己対戦の集計スクリプト（フェーズBで作成）
├── engine/              # Kaggleからのエンジン/cgライブラリ（.gitignore対象）
└── submission.tar.gz    # ビルド成果物（.gitignore対象）
```

## 参考リンク

- コンペ: https://www.kaggle.com/competitions/pokemon-tcg-ai-battle
- 初心者ガイド: https://www.kaggle.com/code/ichigoe/beginner-guide-from-deck-list-to-first-valid-sub
- cabtエンジンAPI: https://matsuoinstitute.github.io/cabt/
- 有志のメタ分析/評価ツール: https://github.com/wmh/ptcg-abc
